---
type Props = {
  className?: string;
};

const { className = "" } = Astro.props as Props;
---

<h3
  class={`text-[clamp(1rem,2vw,1.25rem)] font-bold text-white transition-colors duration-300 group-hover:text-brand-blue-deep ${className}`}
>
  <slot />
</h3>
